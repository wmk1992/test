<!--pages/game/lightOn.wxml-->
  <text class="title">{{title}}</text>
  <text> 计步：{{count}}</text>
  <text class="titleright">计时：{{time}}</text>
  <button bindtap="restart" type="primary" size="default" >重新开始</button>  

<view class="container">
    <view class="table">
        <view class="tr">
            <view id='1' class="{{cs[1]}}" bindtap='touch'></view>
            <view id='2' class="{{cs[2]}}" bindtap='touch'></view>
        </view>
        <view class="tr">
            <view id='3' class="{{cs[3]}}" bindtap='touch'></view>
            <view id='4' class="{{cs[4]}}" bindtap='touch'></view>
        </view>
    </view>
</view>
